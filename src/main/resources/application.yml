spring:
  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    url: ${DB_URL}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: create
  jwt:
    secret: "dkqorhvmekgkwlaksdltlrksdpanjfajrdmausdksehlrpTwlckask"
    access-token-time : 3600000

springdoc:
  swagger-ui:
    path: swagger